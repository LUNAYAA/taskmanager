package com.luna.taskmanager.model;

/**
 * Base class for entities that require auditing information.
 * This class can be extended by other entity classes to inherit common audit fields.
 * Typical audit fields include creation and modification timestamps, creator and modifier identifiers, etc.
 * Currently, this class is empty and should be extended to include relevant auditing fields as per application requirements.
 */
public class Auditable {
    // Fields for audit information (e.g., creationTime, modifiedTime, createdBy, modifiedBy) can be added here.

    // Constructors, getters, setters, and other methods can also be added as needed.
}
